[package]
name              = "rocketsh"
version           = "0.0.0"
edition.workspace = true
license.workspace = true
publish           = false

[dependencies]
shared    = { workspace = true, features = ["rocket"] }
task_core = { workspace = true }

rocket = { workspace = true }
